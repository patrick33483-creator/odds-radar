name: rollback-bad-fills
on: workflow_dispatch
jobs:
  r:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - env:
          DEPLOY_SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
        run: |
          install -m 700 -d ~/.ssh
          printf '%s\n' "$DEPLOY_SSH_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H "$DEPLOY_HOST" >> ~/.ssh/known_hosts
          cat > /tmp/rb.py <<'PYEOF'
          import sqlite3, datetime
          db = sqlite3.connect("/opt/odds-radar/data/data.db", timeout=30, isolation_level=None)
          db.execute("PRAGMA busy_timeout=60000")
          db.row_factory = sqlite3.Row
          # 呢次 batch: 2026-09-04 17:26-17:29 UTC
          lo = int(datetime.datetime(2026,9,4,17,26,0,tzinfo=datetime.timezone.utc).timestamp()*1000)
          hi = int(datetime.datetime(2026,9,4,17,30,0,tzinfo=datetime.timezone.utc).timestamp()*1000)
          n = db.execute("SELECT COUNT(*) c FROM research_results WHERE result_source='manual' AND fetched_at>=? AND fetched_at<=?",(lo,hi)).fetchone()['c']
          print(f"about to delete {n} rows")
          for r in db.execute("SELECT match_id, home_score, away_score FROM research_results WHERE result_source='manual' AND fetched_at>=? AND fetched_at<=? LIMIT 10",(lo,hi)):
              print(f"  sample: {r['match_id']} {r['home_score']}-{r['away_score']}")
          db.execute("BEGIN IMMEDIATE")
          cur = db.execute("DELETE FROM research_results WHERE result_source='manual' AND fetched_at>=? AND fetched_at<=?",(lo,hi))
          db.execute("COMMIT")
          print(f"deleted {cur.rowcount}")
          PYEOF
          scp /tmp/rb.py "$DEPLOY_USER@$DEPLOY_HOST:/tmp/rb.py"
          ssh "$DEPLOY_USER@$DEPLOY_HOST" 'sudo -E python3 /tmp/rb.py'
